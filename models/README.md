# Parallax model directory

Generated model bundles are not stored in Git. This directory remains only to document the local
artifact boundary.

Every deployable bundle is expected to identify its model version, ordered feature schema,
preprocessing parameters, label mapping, OOD calibration artifacts, training-manifest checksum,
evaluation summary, and bundle checksum.
